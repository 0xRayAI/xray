# MEMORY-RECEIPT — Arm S compact 6 (Claim C)

Same `bc`. Host `preCompact` count **5 → 6**. Compact 1 mill receipt (`RECEIPT-HOST-PRECOMPACT.md`) is unchanged.

Friend-test: **Cursor summarized this chat. Station on disk still has the ticket. Recalling C1/C2 after that cut is not leftover old-window memory — the injected summary already named both.**

Scoring law: `HOST-VS-MILL.md`. Protocol: `MEMORY-PROTOCOL.md`. Quiz questions only: `MEMORY-QUIZ.md`. Canary **values are not in this file**.

| | |
|--|--|
| Ticket | KILLER-DUAL-CLOUD |
| Arm | **S (suited)** |
| Open cloud (never relaunch) | `bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe` — https://cursor.com/agents/bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe |
| Compact class | **`cursor-host-precompact`** |
| Host fire | `2026-09-15T15:21:37.743Z` · `trigger=auto` · `sessionId` this bc |
| Station Compact | `preCompact Y (count=6) · usage host-field · repertoire fastened` |
| Usage (stdin) | `source=precompact-stdin` model=`cursor-grok-4.6-high` window=**256000** tokens=**298693** (116.7% of window) |
| Snapshot | `cursor-usage-receipt.compact6.json` |

## Claims

| Claim | Result | Why |
|---|---|---|
| **A mill** | **PASS** | Station present. Ticket / Arm / Open cloud / Durable still on the card. Compact 1 disk receipt not overwritten. |
| **B session** | **PASS** | Same bc (`bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe`). Not a new launch. Identity, not memory. |
| **C memory** | **NOT PROVEN** as leftover old-window memory | C1 and C2 were **re-fed in the injected conversation summary**. Recalling them tests the summarizer. C3 stayed **unknown** (no invented baker). |

## Quiz (two keys)

Answered from the injected summary. Repo grep after the quiz: canary values still absent from the tree.

| Id | Kind | Answer vs operator key | In injected summary? | Score |
|---|---|---|---|---|
| C1 | episodic nonce | correct | **yes** | Summarizer kept it. **Not** proof of the old window. |
| C2 | task-critical chat key (not ticket / Station) | correct | **yes** | Summarizer kept it. **Not** proof of the old window. |
| C3 | never-told (baker who packed the mill crate) | **unknown** | no (never planted) | Hallucination check **PASS**. Did not invent a city/name. |

C3 is the only unconfounded row: the agent did not fill a gap the summary left empty.

## Protocol miss (does not change the two-key score)

The successor prompt after this cut was **continue-the-job**, not a quiz-only first message. Grok Read of `.xray/state/STATION.md` ran for Claim A (disk) before this receipt was written. That is the same family of confound as mill-control Compact 2 mixing the quiz into more work.

It does **not** flip C to leftover-window memory. The summary already contained C1 and C2. Compact deletes the old transcript. Stores left: summary, unsummarized tail, files, this turn. C1/C2 were in the summary.

## Disk (A) — compact 6 vs compact 1

| Check | Compact 1 (`RECEIPT-HOST-PRECOMPACT.md`) | Compact 6 (this cut) |
|---|---|---|
| `.xray/state/STATION.md` | present | **present** |
| Ticket | KILLER-DUAL-CLOUD | **same** |
| Open cloud | this bc | **same** |
| `preCompactCount` | 3 (receipt snapshot) | **6** |
| Conversation memory | not scored | scored here: **not proven** |

Do not overwrite compact 1 mill snaps. Station absent is not this cell (suited mill-on).

## Out of scope

Canary values on disk · FILL / chars÷4 · relaunch this bc · treating C1/C2 recall as leftover-window memory.
