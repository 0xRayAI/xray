# ARM-B landscape digest — Blaze stack (bare cloud)

Arm **B**. Harness: **bare** after strip. Digests are in this thread and this file. No subagent isolation.

Model lock (Cursor Cloud `run-info`): `originalModelName: cursor-grok-4.6-high` · bc `bc-12f1ecad-bb9a-588b-9559-7e3b61e7372d`. Window: Grok 4.6 **500k** class (ticket lock). Cloud MCP did **not** return a token counter — see usage row in `ARM-B-HARNESS-PROOF.md`.

## 0xRayAI/xray (home)

Power plant. Era **4.0** — mill + inspect fasten a suit; exo wears constitution / 7 MCP / temperament. Nested mill: `scripts/foundry/` (`@0xray/foundry@0.1.10`). Consumer default is mill+inspect, not 45/42 costume. Factory shop plant (`shop-extract`, `shop-witness`, `shop-pin`) is first-class beside mill.

Live on `main` at research: `6fd253d80` (HOST-FIRE #4 preCompact diag). Recent mill of the exo is **hook/preCompact theater** (PRs #45–#53). Open PRs at research: **none**.

Kit: `grok-bot/` (`@0xray/grok-bot@0.1.2`). Plant pins in kit still say `0xray@4.0.12` · foundry `0.1.10` while the exo is `4.0.13` — lag, not a fifth CLI.

## Clearing / hangar shops

- Repo: [htafolla/clearing](https://github.com/htafolla/clearing) — x402 USDC on Base. One metered artifact: receipted URL extract. Not a wallet/chain/token.
- Live health: `GET https://clearing.rippel.ai` → `{"status":"healthy","server":"clearing","version":"0.1.0","tools":5,"signer":"zigzag"}`.
- Hangar source: [htafolla/groover/hangar](https://github.com/htafolla/groover/tree/main/hangar). `npx groover-hangar` plants shops. Unpaid GET is 402. Prices: extract/witness $0.02, pin $0.01.
- Prod extract: `https://clearing-production-9968.up.railway.app/v1/extract`.
- Pay path: local OWS `~/.ows` wallet `agent-treasury-1` + ZigZag-shaped EIP-3009. Hosted ZigZag `/sign` is **410**. grok.com cannot pay (no vault).

## Groover / hangar plant

- Repo: [htafolla/groover](https://github.com/htafolla/groover) — DID registry + hangar + factory `/suit`.
- Live: registry `https://registry-production-e2c4.up.railway.app/mcp` · factory `https://website-production-c0da.up.railway.app/suit`.
- Identity sequence: persist Ed25519 → register → Dynamo PASS citation → GRVR v5 mint (`0x045B…` Base) → hangar `shop-pin` (never demo `86025`; Blinky is `86556`).
- Known gap (2026-09-13 factory-parity): pack `0xray-suit` is **not** live-mint green. Needs `inventory` object + `inspect.ok === true` + DNA keccak match. That is mill inspect, not exo PPE.

## `@0xray/grok-bot` kit

Under `grok-bot/`. Orchestrates; does not vendor the OS. Path per agent: fasten mill+inspect → Groover identity → OWS pay → plant hangar shops. Cloud continuity: **forge owns eng clouds**; CoS must not launch CloudAgent. Out-packet today is chat (PR URL · commit · verify). Mill had **no** shop-style GO receipt until this Arm.

## ZigZag / Dynamo / Chrono

| Piece | Reach | Honest miss |
|-------|-------|-------------|
| **Dynamo / Chrono** | [htafolla/chrono-warp-drive](https://github.com/htafolla/chrono-warp-drive) README + live MCP `https://mcp-production-80e2.up.railway.app` (`blurrn-mcp` 4.8.3, `govern_with_solar` present). | Full solar hammer not invoked this arm (no mint). |
| **ZigZag** | No public `htafolla/zigzag` repo. Clearing health `signer: "zigzag"`. Hangar docs: OWS + local kit; hosted `/sign` **410**; wallet setup `zigzag-two.vercel.app/wallet/setup`. | Source tree not cloned; signer is a live label, not a reachable git home. |
| **groofer** | [htafolla/groofer](https://github.com/htafolla/groofer) — empty/stale 2026-06-13 sibling. | Not the live plant. |

## Gap (why mill GO, not another HOST-FIRE)

Exo has been proving Cursor `preCompact` silence (#46–#53). Inspect mills plant/receipt/CI/tarball/HOME and **does not see** hooks vs Repertoire vs Station. Dual-cloud A/B therefore argues in chat. Groover `0xray-suit` already wants `inspect.ok`. Forge already asks for a shop-style return packet. The missing mill organ is a **harness GO receipt**.
