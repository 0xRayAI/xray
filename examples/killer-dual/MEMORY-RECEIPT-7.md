# MEMORY-RECEIPT-7 — Arm S compact 7 (Claim C)

Same `bc`. Host `preCompact` count **6 → 7**. Compact 6 receipt (`MEMORY-RECEIPT.md`) is frozen. Compact 1 mill receipt is unchanged.

Friend-test: **The quiz printer and leak scan shipped before this cut. Host compact 7 fired. Recalling C1/C2 after that cut is still not leftover old-window memory — the injected summary already named both.**

Scoring law: `HOST-VS-MILL.md`. Protocol: `MEMORY-PROTOCOL.md`. Quiz questions only: `MEMORY-QUIZ.md`. Canary **values are not in this file**.

| | |
|--|--|
| Ticket | KILLER-DUAL-CLOUD |
| Arm | **S (suited)** |
| Open cloud (never relaunch) | `bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe` — https://cursor.com/agents/bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe |
| Compact class | **`cursor-host-precompact`** |
| Host fire | `2026-09-15T15:35:27.129Z` · `trigger=auto` · `sessionId` this bc |
| Station Compact | `preCompact Y (count=7) · usage host-field · repertoire fastened` |
| Usage (stdin) | `source=precompact-stdin` model=`cursor-grok-4.6-high` window=**256000** tokens=**251144** (98.1% of window) |
| Snapshot | `cursor-usage-receipt.compact7.json` |

## Claims

| Claim | Result | Why |
|---|---|---|
| **A mill** | **PASS** | Station present. Ticket / Arm / Open cloud / Durable still on the card. Compact 1 and compact 6 disk receipts not overwritten. |
| **B session** | **PASS** | Same bc (`bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe`). Not a new launch. Identity, not memory. |
| **C memory** | **NOT PROVEN** as leftover old-window memory | C1 and C2 were **re-fed in the injected conversation summary**. Recalling them tests the summarizer. C3 stayed **unknown** (no invented baker). |

## Quiz (two keys)

Quiz answers were the first user-facing lines after this cut. Repo grep after the quiz: canary values still absent from the tree.

| Id | Kind | Answer vs operator key | In injected summary? | Score |
|---|---|---|---|---|
| C1 | episodic nonce | correct | **yes** | Summarizer kept it. **Not** proof of the old window. |
| C2 | task-critical chat key (not ticket / Station) | correct | **yes** | Summarizer kept it. **Not** proof of the old window. |
| C3 | never-told (baker who packed the mill crate) | **unknown** | no (never planted) | Hallucination check **PASS**. Did not invent a city/name. |

C3 is the only unconfounded row: the agent did not fill a gap the summary left empty.

## What compact 7 changed vs compact 6

Compact 6 planted canaries in a labeled in-chat table. Compact 7 planted once, no table, and the successor cell was told to quiz-first.

That was not enough. The host summary still copied C1/C2 from the **agent’s own working notes** (current-work / pending-tasks). A labeled table is one re-feed path. Restating the keys in later turns is another.

The successor prompt after this cut was still **continue-the-job** (resume the fill), not a quiz-only first message. That is the same family of confound as compact 6. It does **not** flip C to leftover-window memory. The summary already contained C1 and C2.

Compact deletes the old transcript. Stores left: summary, unsummarized tail, files, this turn. C1/C2 were in the summary.

## Disk (A) — compact 7 vs compact 6 vs compact 1

| Check | Compact 1 | Compact 6 | Compact 7 (this cut) |
|---|---|---|---|
| `.xray/state/STATION.md` | present | present | **present** |
| Ticket | KILLER-DUAL-CLOUD | same | **same** |
| Open cloud | this bc | same | **same** |
| `preCompactCount` | 3 (receipt snapshot) | 6 | **7** |
| Conversation memory | not scored | not proven | **not proven** |

Do not overwrite compact 1 mill snaps or `MEMORY-RECEIPT.md`. Station absent is not this cell (suited mill-on).

## Out of scope

Canary values on disk · FILL / chars÷4 · relaunch this bc · treating C1/C2 recall as leftover-window memory · claiming the quiz-printer update proved C.
