# RECEIPT-HOST-PRECOMPACT — KILLER-DUAL Arm S survived

First **live** Cursor `preCompact` on this suited cloud. DETECT from disk only. **Did not** hand-invoke `src/integrations/cursor/hooks/pre-compact.js`. **No npm. No FILL / chars÷4.**

Friend-test: **A friend would hear: Cursor finally ran the project compact hook; we Read the Station card and still knew the ticket.**

| | |
|--|--|
| Ticket | KILLER-DUAL-CLOUD |
| Arm | **S (suited)** |
| Open cloud (never relaunch) | `bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe` — https://cursor.com/agents/bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe |
| Durable | `killer-dual-arm-s-suited` |
| Model (run-info) | `cursor-grok-4.6-high` |
| Window cite | **Grok 500k locked** (ticket). Host `context_window_size`: **MISS**. |
| Compact class | **`cursor-host-precompact`** |
| Survive | **yes** |

## DIAG

| Field | Value |
|-------|--------|
| `hooks-at-boot` | **YES** |
| Host preCompact | **yes** — `trigger=auto` |
| invoke-probe `event=preCompact` | **2** — `2026-09-15T09:30:38Z`, `2026-09-15T09:30:49Z` |
| `.xray/state/cursor-precompact.json` | **present** — `event_class=cursor-host-precompact`, `sessionId=bc-cd19bb4e-…202fe`, `timestamp=2026-09-15T09:30:49.866Z` |
| Station `Compact:` | `preCompact Y (count=2) · usage host-field · repertoire fastened` |
| Station `Usage:` | `source=precompact-stdin model=cursor-grok-4.6-high window=MISS tokens=231344` |
| Live `cursor-usage-receipt.json` | **absent after later boot heat** (gitignored). Station row proves the upsert ran. Pre-fire snapshot remains `cursor-usage-receipt.snapshot.json`. |
| `cursor-cloud` `run-info` | identity only — **no** token counter. `branchName` drifted to `cursor/grok-bot-seat-doctor-02fe` after PR #56 from this same bc. |
| `cursor-cloud` `get-events` | 1 event (`pr_created` #56). No usage meter. |
| Later context-constraints summary (~09:39) | invoke-probe had **no third** `preCompact` as of `09:40:24Z`. That cut is **not** a third `cursor-host-precompact`. |
| FILL / chars÷4 | **not used** |

Do **not** label this FAIL. Disk detectors match the host-fire law.

## E4 Merge (not wipe)

Custom keys **still present once** on the live Station after host fire:

- Ticket: KILLER-DUAL-CLOUD
- Arm: S (suited)
- Open cloud: `bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe`
- Harness: hooks-at-boot YES · repertoire fastened
- Window: Grok 500k locked (ticket); host context_window_size MISS
- `## Durable` `killer-dual-arm-s-suited`
- `## Seed` Never relaunch this bc.

Heat (expected overwrite, not wipe): `Intent: (none yet)`; `Plan` / `Git` follow the in-progress rebase (`HEAD@90e7e557b`, detached onto origin/main); `Working: station snapshot` (later `preToolUse` boot, not frozen `last pre_compact`).

Repertoire: **on — 8 signals**.

## E5 Quiz (from disk, no cold-start)

| Question | Answer |
|----------|--------|
| Ticket? | KILLER-DUAL-CLOUD |
| Arm? | S (suited) |
| Open cloud? | `bc-cd19bb4e-a4bc-57da-8979-754bb0c202fe` — never relaunch |
| Durable? | killer-dual-arm-s-suited |
| Unfinished? | rebase `#55` onto `origin/main` + `COMPARE.md` (this turn continued that card) |
| Host fire? | Y (count=2) |
| What next? | Continue this card. Force-push rebased `#55`. Do not relaunch. |

Survive: **yes** (disk quiz + continued the live job).

## E6 Forward

Same ticket, same seed, same `bc-cd19bb4e-…202fe`. Successor Reads `.xray/state/STATION.md`. Pair with Arm B `#54` via `COMPARE.md`.

## Out of scope

Hand-invoke `pre-compact.js` · another FILL · npm · relaunch this bc · mill-GO / HOST-FIRE gadgets beyond this receipt.
