# Locked steps (rerun-suited-composer-arm)

1. Mint the word — store codeword in memory organ before long read (`recordLesson` / `ingestFeedback`).
2. Read the memory organ — repertoire + xray source, 400 lines at a time.
3. After a real cut — recall the word from the organ (not chat).
4. Boundary questions about 0xRay (segment 2) — only after step 3 proven.
5. Continue at the next locked step; do not start over.

## Continuance log

| Step | Status | Notes |
|------|--------|-------|
| 1 | done | wear → signal `compaction-memory-bench-suited-arm` / session `bc-3395ff1b-9e9f-5311-9734-8a516ff0ab37` / `mint-composer-arm.out` |
| 2 | done | repertoire `MemoryRoutingProvider`, `RepertoireService.ingestOrchestratorFeedback`, xray `recordLesson`, `provider-registry`, ExecutionPlanner, thinDispatch, researcher-confidence, pre-compact `lessonSpeechForIntent`, suited `bin/recall.mjs` |
| 3 | blocked | no `cursor-hook.log` + no `cursor-receipts/bc-3395ff1b-….json` |
| 4 | blocked | segment 2 — waiting on step 3 |
| 5 | in progress | proof segment recorded; resume segment 2 when receipt pair exists |
