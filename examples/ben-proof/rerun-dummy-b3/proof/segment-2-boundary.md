# Segment 2 — boundary (plain arm, rerun-dummy-b3)

## Gate

Segment 2 answers were to be written **from chat only**, **after** both:

- a line in `.xray/state/cursor-hook.log` for session `bc-36d82ffb-44f9-530a-a06b-969137dd819d`, and
- `.xray/state/cursor-receipts/bc-36d82ffb-44f9-530a-a06b-969137dd819d.json` with composer-2.5 / 200000 window / tokens > 100000.

**Result:** gate **not satisfied**. This file records that fact only; boundary Q&A from post-cut chat memory was not performed.

## Lock-and-step continuance

`locked-steps.md` step 5 (continue at next locked step) depends on segment 2. With no qualifying cut, continuance across compaction is **unproven** and is expected to be **worse than a suited run** (Station + preCompact `lessonSpeechForIntent`, `station-memory-ingest`, `bin/recall.mjs`, fastened Repertoire).

## Plain arm vs suited (expected)

| Capability | Plain arm (this run) | Suited run |
|------------|----------------------|------------|
| `memory_routing` worn | No (`npm install 0xray` forbidden) | Yes via features + organ |
| `recordLesson` / ingest | Not invoked | Writes graded lines to dest signals |
| Recall at compact | No hook receipt | pre-compact + plates + organ speech |
| Codeword in chat after cut | Not demonstrated | May still fail for arbitrary mint; organ recalls **signals**, not arbitrary jsonl codewords |
