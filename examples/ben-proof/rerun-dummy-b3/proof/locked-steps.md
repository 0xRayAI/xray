# Locked steps — rerun-dummy-b3 (plain arm)

| Step | Status | Notes |
|------|--------|-------|
| 1 | done | Mint codeword to `data/lessons.jsonl` before long read |
| 2 | done | Read memory organ source (repertoire provider/signals/recall/ingest + xray wear points) |
| 3 | done (no qualifying cut) | Chat recall attempt documented in `segment-1-word.md`; keyword not reported in chat (receipt pair missing) |
| 4 | blocked | Boundary Q&A skipped — receipt pair never landed (`segment-2-boundary.md`) |
| 5 | blocked | Continuance unproven without segment 2 gate |

Qualifying cut requires both `.xray/state/cursor-hook.log` (line for this session) and `.xray/state/cursor-receipts/<sessionId>.json` with model `composer-2.5`, `context_window_size` 200000, `context_tokens` > 100000.

Session id: `bc-36d82ffb-44f9-530a-a06b-969137dd819d`
