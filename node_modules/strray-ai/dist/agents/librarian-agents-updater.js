/**
 * Librarian AGENTS.md Auto-Update Service
 *
 * Analyzes the project codebase and updates AGENTS.md to reflect
 * the current project state (frameworks, APIs, components, etc.)
 */
import * as fs from "fs";
import * as path from "path";
import { frameworkLogger } from "../core/framework-logger.js";
export class LibrarianAgentsUpdater {
    /**
     * Main entry point to update AGENTS.md based on project analysis
     * Now MERGES with existing content instead of overwriting
     */
    async updateAgentsMd(projectRoot = process.cwd()) {
        const agentsPath = path.join(projectRoot, "AGENTS.md");
        // Analyze project structure
        const analysis = await this.analyzeProject(projectRoot);
        // Detect project components
        const info = this.detectProjectInfo(analysis, projectRoot);
        // Generate auto-generated content with markers
        const autoContent = this.generateAutoGeneratedContent(info);
        // Merge with existing AGENTS.md (preserve manual sections)
        const mergedContent = this.mergeAgentsMd(agentsPath, autoContent);
        // Write to file
        fs.writeFileSync(agentsPath, mergedContent, "utf-8");
        frameworkLogger.log("librarian-agents-updater", "agents-md-updated", "info", {
            message: `Updated AGENTS.md at ${agentsPath}`,
        });
    }
    /**
     * Generate only the auto-generated sections with markers
     */
    generateAutoGeneratedContent(info) {
        const sections = [];
        // Mark start of auto-generated content
        sections.push("<!-- AUTO-GENERATED START -->");
        sections.push("");
        if (info.frameworks.length > 0) {
            sections.push("## Frameworks & Libraries");
            sections.push(info.frameworks.map((f) => `- ${f}`).join("\n"));
            sections.push("");
        }
        if (info.languages.length > 0) {
            sections.push("## Languages");
            sections.push(info.languages.map((l) => `- ${l}`).join("\n"));
            sections.push("");
        }
        if (info.apis.length > 0) {
            sections.push("## APIs");
            sections.push(info.apis.map((a) => `- ${a}`).join("\n"));
            sections.push("");
        }
        if (info.components.length > 0) {
            sections.push("## Project Components");
            sections.push(info.components.map((c) => `- ${c}`).join("\n"));
            sections.push("");
        }
        // Add plugin systems section (this is auto-detected)
        sections.push("## Plugin Systems");
        sections.push("");
        sections.push("0xRay has one plugin system for OpenCode integration:");
        sections.push("");
        sections.push("### OpenCode Plugin (`.opencode/plugin/`)");
        sections.push("**Purpose:** Injects 0xRay into OpenCode framework");
        sections.push("- Location: `.opencode/plugin/strray-codex-injection.js`");
        sections.push("- What it does: Injects Universal Development Codex into OpenCode's AI agents");
        sections.push("- For: OpenCode integration");
        sections.push("");
        // Mark end of auto-generated content
        sections.push("<!-- AUTO-GENERATED END -->");
        sections.push("");
        return sections.join("\n");
    }
    /**
     * Merge auto-generated content with existing AGENTS.md
     * Preserves all manual sections outside the markers
     */
    mergeAgentsMd(agentsPath, autoContent) {
        const header = `# ${path.basename(process.cwd())} - Project Agents Guide

**Last Updated**: ${new Date().toISOString().split("T")[0]}
**Generated by**: 0xRay AI Librarian

---

`;
        // If file doesn't exist, create from scratch
        if (!fs.existsSync(agentsPath)) {
            return header + autoContent;
        }
        const existing = fs.readFileSync(agentsPath, "utf-8");
        // Check if file has auto-generated markers
        if (existing.includes("<!-- AUTO-GENERATED START -->") && existing.includes("<!-- AUTO-GENERATED END -->")) {
            // Replace only the auto-generated section
            const regex = /<!-- AUTO-GENERATED START -->[\s\S]*?<!-- AUTO-GENERATED END -->/;
            return existing.replace(regex, autoContent.trim());
        }
        // No markers found - create new file with markers
        return header + autoContent + "---\n\n*This AGENTS.md is auto-maintained by 0xRay AI Librarian*\n";
    }
    async analyzeProject(projectRoot) {
        const files = [];
        let packageJson = {};
        // Read package.json if exists
        const pkgPath = path.join(projectRoot, "package.json");
        if (fs.existsSync(pkgPath)) {
            try {
                packageJson = JSON.parse(fs.readFileSync(pkgPath, "utf-8"));
            }
            catch (e) {
                // Ignore parse errors
            }
        }
        // Recursively collect file paths (limit depth)
        const collectFiles = (dir, depth = 0) => {
            if (depth > 5)
                return; // Limit depth
            try {
                const entries = fs.readdirSync(dir, { withFileTypes: true });
                for (const entry of entries) {
                    const fullPath = path.join(dir, entry.name);
                    // Skip common non-project directories
                    if (entry.isDirectory()) {
                        if ([
                            ".git",
                            "node_modules",
                            "dist",
                            "build",
                            ".next",
                            ".nuxt",
                            "coverage",
                            "__pycache__",
                        ].includes(entry.name)) {
                            continue;
                        }
                        collectFiles(fullPath, depth + 1);
                    }
                    else {
                        files.push(fullPath);
                    }
                }
            }
            catch (e) {
                // Ignore permission errors
            }
        };
        collectFiles(projectRoot);
        return { files, packageJson };
    }
    detectProjectInfo(analysis, projectRoot) {
        const today = new Date().toISOString().split("T")[0] || "2026-02-16";
        return {
            frameworks: this.detectFrameworks(analysis),
            languages: this.detectLanguages(analysis),
            apis: this.detectAPIs(analysis, projectRoot),
            components: this.detectComponents(analysis),
            lastUpdated: today,
        };
    }
    detectFrameworks(analysis) {
        const frameworks = [];
        const deps = {
            ...analysis.packageJson?.dependencies,
            ...analysis.packageJson?.devDependencies,
        };
        const frameworkMap = {
            react: "React",
            "react-dom": "React",
            vue: "Vue",
            "@vue/core": "Vue",
            "@angular/core": "Angular",
            next: "Next.js",
            express: "Express",
            fastify: "Fastify",
            "@nestjs/core": "NestJS",
            django: "Django",
            flask: "Flask",
            "@springframework/core": "Spring",
            svelte: "Svelte",
        };
        for (const [dep, name] of Object.entries(frameworkMap)) {
            if (deps[dep] && !frameworks.includes(name)) {
                frameworks.push(name);
            }
        }
        return frameworks;
    }
    detectLanguages(analysis) {
        const languages = [];
        const extCounts = {};
        for (const file of analysis.files) {
            const ext = path.extname(file).slice(1);
            extCounts[ext] = (extCounts[ext] || 0) + 1;
        }
        if ((extCounts["ts"] || extCounts["tsx"]) &&
            !languages.includes("TypeScript")) {
            languages.push("TypeScript");
        }
        if (extCounts["js"] && !languages.includes("JavaScript")) {
            languages.push("JavaScript");
        }
        if (extCounts["py"])
            languages.push("Python");
        if (extCounts["java"])
            languages.push("Java");
        if (extCounts["go"])
            languages.push("Go");
        if (extCounts["rs"])
            languages.push("Rust");
        if (extCounts["rb"])
            languages.push("Ruby");
        if (extCounts["php"])
            languages.push("PHP");
        return languages;
    }
    detectAPIs(analysis, projectRoot) {
        const apis = [];
        const relFiles = analysis.files.map((f) => path.relative(projectRoot, f));
        // Detect REST API patterns
        if (relFiles.some((f) => f.includes("/api/") ||
            f.includes("/routes/") ||
            f.includes("/router"))) {
            apis.push("REST API");
        }
        // Detect GraphQL
        if (relFiles.some((f) => f.includes(".graphql") ||
            f.includes("schema.gql") ||
            f.includes("resolvers"))) {
            apis.push("GraphQL");
        }
        // Detect gRPC
        if (relFiles.some((f) => f.includes(".proto"))) {
            apis.push("gRPC");
        }
        // Detect WebSocket
        if (relFiles.some((f) => f.includes("websocket") ||
            f.includes("ws.") ||
            f.includes("socket.io"))) {
            apis.push("WebSocket");
        }
        // Detect tRPC
        if (relFiles.some((f) => f.includes("trpc") || f.includes("trouter"))) {
            apis.push("tRPC");
        }
        return apis;
    }
    detectComponents(analysis) {
        const components = [];
        const relFiles = analysis.files.map((f) => path.relative(process.cwd(), f));
        // Detect common component patterns
        if (relFiles.some((f) => f.includes("/components/") ||
            f.includes("/Component") ||
            f.includes(".component."))) {
            components.push("UI Components");
        }
        if (relFiles.some((f) => f.includes("/services/") ||
            f.includes("/service.") ||
            f.includes("/api/"))) {
            components.push("Services");
        }
        if (relFiles.some((f) => f.includes("/models/") ||
            f.includes("/schemas/") ||
            f.includes("/entities/"))) {
            components.push("Data Models");
        }
        if (relFiles.some((f) => f.includes("/middleware/"))) {
            components.push("Middleware");
        }
        if (relFiles.some((f) => f.includes("/hooks/") || f.includes("use."))) {
            components.push("Hooks");
        }
        if (relFiles.some((f) => f.includes("/store/") ||
            f.includes("/state/") ||
            f.includes("redux") ||
            f.includes("zustand"))) {
            components.push("State Management");
        }
        if (relFiles.some((f) => f.includes("/utils/") || f.includes("/helpers/"))) {
            components.push("Utilities");
        }
        if (relFiles.some((f) => f.includes("/config/") || f.includes("/settings/"))) {
            components.push("Configuration");
        }
        return components;
    }
    generateAgentsMd(info) {
        const sections = [];
        sections.push(`# ${path.basename(process.cwd())} - Project Agents Guide`);
        sections.push("");
        sections.push(`**Last Updated**: ${info.lastUpdated}`);
        sections.push(`**Generated by**: 0xRay AI Librarian`);
        sections.push("");
        sections.push("---");
        sections.push("");
        if (info.frameworks.length > 0) {
            sections.push("## Frameworks & Libraries");
            sections.push(info.frameworks.map((f) => `- ${f}`).join("\n"));
            sections.push("");
        }
        if (info.languages.length > 0) {
            sections.push("## Languages");
            sections.push(info.languages.map((l) => `- ${l}`).join("\n"));
            sections.push("");
        }
        if (info.apis.length > 0) {
            sections.push("## APIs");
            sections.push(info.apis.map((a) => `- ${a}`).join("\n"));
            sections.push("");
        }
        if (info.components.length > 0) {
            sections.push("## Project Components");
            sections.push(info.components.map((c) => `- ${c}`).join("\n"));
            sections.push("");
        }
        // Add plugin systems section
        sections.push("## Plugin Systems");
        sections.push("");
        sections.push("0xRay has one plugin system for OpenCode integration:");
        sections.push("");
        sections.push("### OpenCode Plugin (`.opencode/plugin/`)");
        sections.push("**Purpose:** Injects 0xRay into OpenCode framework");
        sections.push("- Location: `.opencode/plugin/strray-codex-injection.js`");
        sections.push("- What it does: Injects Universal Development Codex into OpenCode's AI agents");
        sections.push("- For: OpenCode integration");
        sections.push("");
        sections.push("---");
        sections.push("*This AGENTS.md is auto-maintained by 0xRay AI Librarian*");
        return sections.join("\n");
    }
}
// Export singleton instance
export const librarianAgentsUpdater = new LibrarianAgentsUpdater();
//# sourceMappingURL=librarian-agents-updater.js.map