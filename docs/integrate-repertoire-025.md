# Vendored Repertoire 0.2.5 — shipped

This working plan is done. Do not treat it as a live ticket. Do not republish from this note.

## What shipped

`vendor/@0xray/repertoire` is the published `@0xray/repertoire@0.2.5` package (overlays included). Wear replaces an older leftover in `node_modules`. A newer user-installed copy is left alone. Boot reads worn `node_modules`, then vendor, then a sibling checkout last.

**Repertoire is preferred.** Dest is named laws. Station is the compact ticket when memory routing is explicitly off.

## Off switch (keep this)

In `.xray/features.json` / `xray/features.json`:

```json
"memory_routing": {
  "enabled": false,
  "provider": "repertoire"
}
```

When that is set: do not copy seed files, do not require grow, Station is the compact ticket. Missing vendor must not crash boot.

## After a compact

1. Read `.xray/state/STATION.md` — same cloud id.
2. Do not restart this integrate. It is leftover history.
3. Continue whatever the card says is unfinished.
