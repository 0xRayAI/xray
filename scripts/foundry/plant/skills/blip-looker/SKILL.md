---
name: blip-looker
description: Factory-blip looker. Friend hitting replay. Hook → jewel → hold. Ship bar is 8. Not vibe density. Not last-blip inspect. Not costume.
---

# Blip looker

This plant **fastens** the replay seat. Inspect quotes file / duration. Vibe quotes stamp density. Mixer quotes ears. This organ asks: **would a friend hit replay?**

It is not a model. It paints hook, turn, and tag, then scores the Short. Fail closed under 8.

**Seat:** fastened with `blip` + `blip-inspect` + `blip-vibe` when `foundry.json` `"plant": "blip"`. Not a 45-skill dump.

**Types:** `still` · `orb` · `swirl` · `snap` · `waves` · `spark` · `kapow`. Same agent. Same bar.

## Spine

1. **Look** — setup, jewel cut, hold. Three frames, not a poster.
2. **Score** — setup, punch, hold, motion (hook≠turn≠tag), jewel, replay. Mean is 0–10. Kapow also needs the word on the cut.
3. **Bar** — **8 to ship**. Under 8 is FAIL. Do not weaken the bar. Still’s jewel is the plate change, not a brighter frame.
4. **Receipt** — `.xray/blip-looker-receipt.json` (`PASS` / `FAIL` + score + `byType`).

```bash
npx @0xray/foundry blip look
npx @0xray/foundry blip replay
```

A FAIL score is a phrase bug. The turn must hit. The tag must hold. Do not lower the bar.

A friend would hit replay: the hook sets a stamp, the jewel punches, the tag does not die.
