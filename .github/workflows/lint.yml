name: Code Quality

# Folded into 0xRay CI/CD quality job. Keep the file so old badges do not 404.
on:
  workflow_dispatch:

jobs:
  lint:
    name: Folded into 0xRay CI/CD
    runs-on: ubuntu-latest
    steps:
      - run: echo "typecheck and eslint live in the 0xRay CI/CD quality job"
