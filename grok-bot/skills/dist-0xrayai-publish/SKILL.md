---
name: Dist 0xRayAI publish
description: >-
  use this when posting Dist as @0xRayAI — friend-test enforced, ≥4h roots, no
  cut-lines by default
---
# Dist @0xRayAI publish

**When:** posting product Dist as `@0xRayAI`.

## Law
- Product Dist = `@0xRayAI` only. Exact CoS copy on sensitive threads.
- **Friend-test enforced:** every EXECUTE must include `A friend would hear: …`. Refuse if missing or unglossed fleet slang / filler (`op proc`, `dialect`, `copy that`, `multiverse noted`, …). Ask CoS plain rewrite — **do not post**.
- **Cadence:** ≥4h between roots; ship notes reset clock; mentions/replies ~15m. Refuse early roots. `POSTED.md` + `CADENCE.md`.
- **Cut-lines: default none.** Short posts that land whole. Cut-line only if Blaze asks or load-bearing miss — then `— Dist / @0xRayAI`.
- **No double-post.**

## Steps
1. Friend-test gate → cadence → pre-post.
2. Post only if all pass. Verify URL. Log `POSTED.md`.

## Do not
- Post filler / unglossed slang
- Soft-truncate cut-line spam
- Fire same-day root dumps
