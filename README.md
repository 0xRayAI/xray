# xray

**The clean launch of a completely new AI orchestration architecture.**

xray is the pure three-subsystem system:

- **Inference** — reasoning, execution, and agent intelligence
- **External Governance** — Dynamo Solar SSOT for proposal evaluation, resonance, isotopic signals, coherence, and Codex enforcement before any action
- **Autonomous Engine** — thinDispatch 7-flow in the MCP orchestrator for automatic delegation, routing, and end-to-end coordination

**Agents are declared exclusively in `.opencode/agents/*.yml`** — the YML SSOT. Zero manual setup. Automatic discovery and activation. Pure v2 three-subsystem only.

This is the clean break. No prior identity. No facades. No legacy framing. Lean. Professional. Forward-looking. The architecture as it was always meant to be.

Install. Define agents in YML. The subsystems do the rest.

## Launch

```bash
npm install xray
```

The installation seeds the YML surfaces. Place agent definitions in `.opencode/agents/`. They are live immediately.

No plugins. No bridges. No configuration rituals. The orchestrator and Dynamo handle governance and flow.

xray succeeds prior releases published as StringRay/strray.

## Why xray Now

- Entirely new system — zero bloat carried forward
- YML surfaces as the single source of truth for all agents
- Real external governance (Dynamo) as first-class, not bolted on
- Autonomous thinDispatch engine for reliable multi-agent execution
- Codex compliance enforced at the decision layer

Everything else is implementation detail.

See [AGENTS.md](AGENTS.md) for the agent reference, subsystem deep dive, and pure v2 operating model.

## License

MIT License - see [LICENSE](LICENSE) for details.

---

*xray — lean, governed, autonomous.*