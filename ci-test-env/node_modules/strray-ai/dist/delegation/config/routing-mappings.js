/**
 * Routing Mappings Manager
 *
 * Handles loading, validation, and merging of routing mappings.
 * Extracted from task-skill-router.ts as part of Phase 1 refactoring.
 */
import * as fs from "fs";
import * as path from "path";
import { ROUTING_CONFIG } from './routing-config.js';
/**
 * Load mappings from configuration file if available
 */
export function loadMappingsFromConfig() {
    if (!ROUTING_CONFIG.ENABLE_CONFIG_FILE)
        return null;
    try {
        const configPath = path.resolve(process.cwd(), ROUTING_CONFIG.CONFIG_FILE_PATH);
        if (fs.existsSync(configPath)) {
            const content = fs.readFileSync(configPath, "utf-8");
            const parsed = JSON.parse(content);
            // Validate basic structure
            if (Array.isArray(parsed)) {
                return parsed;
            }
        }
    }
    catch (error) {
        // Config file is optional - fall back to hardcoded
        // Log error only in debug mode to avoid noise
        if (process.env.DEBUG_ROUTING === 'true') {
            console.debug('[Routing] Failed to load config file:', error);
        }
    }
    return null;
}
/**
 * Validate mappings structure
 */
export function validateMappings(mappings) {
    const errors = [];
    const warnings = [];
    const seenKeywords = new Map();
    for (let i = 0; i < mappings.length; i++) {
        const mapping = mappings[i];
        // Skip undefined/null entries
        if (!mapping) {
            errors.push(`Mapping at index ${i}: is undefined or null`);
            continue;
        }
        // Check required fields
        if (!mapping.keywords || !Array.isArray(mapping.keywords)) {
            errors.push(`Mapping at index ${i}: missing or invalid 'keywords'`);
        }
        if (!mapping.skill || typeof mapping.skill !== 'string') {
            errors.push(`Mapping at index ${i}: missing or invalid 'skill'`);
        }
        if (!mapping.agent || typeof mapping.agent !== 'string') {
            errors.push(`Mapping at index ${i}: missing or invalid 'agent'`);
        }
        if (typeof mapping.confidence !== 'number' || mapping.confidence < 0 || mapping.confidence > 1) {
            errors.push(`Mapping at index ${i}: invalid 'confidence' (must be 0-1)`);
        }
        // Check for duplicate keywords
        if (mapping.keywords && Array.isArray(mapping.keywords)) {
            for (const keyword of mapping.keywords) {
                const existing = seenKeywords.get(keyword);
                if (existing !== undefined) {
                    warnings.push(`Duplicate keyword '${keyword}' at indices ${existing} and ${i}`);
                }
                else {
                    seenKeywords.set(keyword, i);
                }
            }
        }
    }
    return {
        valid: errors.length === 0,
        errors,
        warnings,
        duplicateCount: warnings.length,
    };
}
/**
 * Merge default mappings with config mappings
 * Config mappings take precedence (override defaults)
 */
export function mergeMappings(defaultMappings, configMappings) {
    // Create a map for deduplication based on skill+agent combination
    const mappingMap = new Map();
    // Add default mappings first
    for (const mapping of defaultMappings) {
        const key = `${mapping.skill}:${mapping.agent}`;
        mappingMap.set(key, mapping);
    }
    // Override with config mappings
    for (const mapping of configMappings) {
        const key = `${mapping.skill}:${mapping.agent}`;
        mappingMap.set(key, mapping);
    }
    // Convert back to array, maintaining priority order
    const merged = Array.from(mappingMap.values());
    // Sort by confidence (highest first) for better matching
    merged.sort((a, b) => b.confidence - a.confidence);
    return merged;
}
//# sourceMappingURL=routing-mappings.js.map