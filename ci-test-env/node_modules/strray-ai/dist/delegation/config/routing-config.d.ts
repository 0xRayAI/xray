/**
 * Routing Configuration
 *
 * Central configuration for task-skill routing system.
 * Extracted from task-skill-router.ts as part of Phase 1 refactoring.
 */
export declare const ROUTING_CONFIG: {
    readonly MIN_CONFIDENCE_THRESHOLD: 0.75;
    readonly MIN_HISTORY_SUCCESS_RATE: 0.7;
    readonly ENABLE_CONFIG_FILE: boolean;
    readonly CONFIG_FILE_PATH: string;
    readonly ENABLE_OUTCOME_TRACKING: boolean;
    readonly ESCALATE_ON_LOW_CONFIDENCE: true;
    readonly MAX_HISTORY_ENTRIES: 1000;
};
export type RoutingConfig = typeof ROUTING_CONFIG;
//# sourceMappingURL=routing-config.d.ts.map