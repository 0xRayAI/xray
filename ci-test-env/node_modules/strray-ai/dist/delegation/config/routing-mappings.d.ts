/**
 * Routing Mappings Manager
 *
 * Handles loading, validation, and merging of routing mappings.
 * Extracted from task-skill-router.ts as part of Phase 1 refactoring.
 */
import type { RoutingMapping, ValidationResult } from './types.js';
/**
 * Load mappings from configuration file if available
 */
export declare function loadMappingsFromConfig(): RoutingMapping[] | null;
/**
 * Validate mappings structure
 */
export declare function validateMappings(mappings: RoutingMapping[]): ValidationResult;
/**
 * Merge default mappings with config mappings
 * Config mappings take precedence (override defaults)
 */
export declare function mergeMappings(defaultMappings: RoutingMapping[], configMappings: RoutingMapping[]): RoutingMapping[];
//# sourceMappingURL=routing-mappings.d.ts.map