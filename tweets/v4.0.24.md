# 0xRay v4.0.24 — hot-swap and the memory organ

**Status:** LIVE on npm `0xray@4.0.24` · `@0xray/repertoire@0.2.8`  
**Tag:** `v4.0.24` on `44f86c76e` (published `gitHead`)  
**Posted:** not yet

---

## Announcement

0xRay 4.0.24 is on npm.

Exo, not a catalog. A factory stamps a skeleton (Inference, External Governance, Autonomous Engine) and fastens a mill. Code mill, music mill (`@0xray/blip` sound), agents across TUIs and LLMs wear it and operate.

Hot-swappable compaction: compaction and host change are the same cut. The station card (`.xray/state/STATION.md`) is the handoff. The next turn continues the card.

Memory organ: finished speech and reflections become lessons on signals. Kernel and activity logs are heat (`last_seen`) only. Unnamed speech mints a signal. Caps: 20 hot lesson lines, 20 retained ids (insertion order), 24 learned proposed signals. Factory laws stay.

Plates: one INPUT/PROCESSING/OUTPUT schematic per pipeline in `docs-site/docs/plates/`. Station keeps one pointer line. Recall injects that one schematic. A lesson is an episode. A plate is the machine.

Repertoire 0.2.8 is the organ package, vendored in 4.0.24.

```
npm install 0xray
```

---

## Post (plain text — do not send from the release script)

0xRay 4.0.24 is on npm.

Exo, not a catalog. A factory stamps a skeleton — Inference, External Governance, Autonomous Engine — and fastens a mill. A code mill and a music mill (@0xray/blip sound). Agents across TUIs and LLMs wear it and operate.

Hot-swappable compaction: compaction and a host change are the same cut. The station card (.xray/state/STATION.md) is the handoff. The next turn continues the card.

The memory organ turns finished speech and reflections into lessons on signals. Kernel and activity logs are heat (last_seen) only. Speech that names nothing mints a signal. Caps: 20 hot lesson lines, 20 retained ids in insertion order, 24 learned proposed signals. Factory laws stay.

Plates: one INPUT / PROCESSING / OUTPUT schematic per pipeline, in docs-site/docs/plates/. Station keeps one pointer line. Recall injects that one schematic. A lesson is an episode. A plate is the machine.

Repertoire 0.2.8 is the organ package, vendored in 4.0.24.

npm install 0xray
