🎉 0xRay v3.3.0 is LIVE — Pluggable Memory Routing!

✨ MemoryRoutingProvider — provider-agnostic architecture with Repertoire as first plug-in
🧩 Swap providers via features.json: change module_path, zero code changes required
🛡️ Runtime config validation + features.schema.json — catch misconfiguration at load time
🔧 Per-task ingestFeedback (was aggregate) — provider learning loop now accurate per-task
✅ 3,221 tests passing, 0 failures, 24 memory-routing integration tests

```
npm install 0xray@latest
```

What 0xRay is: governance OS that prevents AI coding mistakes — orchestrates agents, enforces codex rules, now extensible via pluggable memory routing providers.

#0xRay #AIOps #DevTools #MemoryRouting #NPM
