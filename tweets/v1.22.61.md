🎉 0xRay v1.22.61 is LIVE - Grok CLI First-Class Integration!
✨ PreToolUse hook now calls real applyDecisionMatrix + Solar governance
📦 MCP servers registered properly via `grok mcp add` (governance + skills)
✅ Robust entrypoint detection — no more fragile import.meta.url failures
🛡️ Graceful degradation when internal skill MCPs are unavailable
🧪 Direct initialize + tools/list verified for both strray servers
```
npm install strray-ai@latest
```
What 0xRay is: governance and skills that actually enforce in Grok, not just suggest.
#0xRay #Grok #DevTools #MCP
