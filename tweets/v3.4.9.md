🎉 0xRay v3.4.9 is LIVE — Orchestrator Honesty!

⚠️ `orchestrate-task` no longer fake-completes — implementation agents defer; `success: false` until you delegate via Task
🔍 `analyze-complexity` NaN fixed — real scores, per-task lead-dev plans
🗣️ Hermes `-z` governance voters — same path as field cron; `HERMES_BIN` + `HERMES_HOME`
✅ 3,253 tests passing

```
npm install 0xray@latest
```

The suit stops lying: consult runs in MCP; implementation runs on the host. Jelly P1 unblocked.

#0xRay #AIOps #DevTools #MCP #NPM #Hermes

---

## Short / alt

0xRay v3.4.9 — the orchestrator suit stops lying.

No more fake ✅ on `orchestrate-task`. No more NaN plans. Hermes `-z` voters aligned with production.

`npm install 0xray@latest`

#0xRay #MCP