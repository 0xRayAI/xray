# 0xRay v3.5.1 + @0xray/repertoire v0.1.3 — diff since v3.4.1 / 0.1.1

**Status:** LIVE on npm · tags `v3.5.1` / `@0xray/repertoire@0.1.3`  
**Last X post:** 0xRay v3.4.1 + @0xray/repertoire@0.1.1  
**Posted:** not yet

---

## Tweet (primary)

Since **v3.4.1** + **repertoire 0.1.1**:

🎉 **0xRay v3.5.1** + **@0xray/repertoire v0.1.3** are LIVE

**Brain (all 4 bridges):**
✨ Honest deferrals — `orchestrate` ≠ done (3.4.9)
🧭 Routing SSOT + phased plans (3.4.10)
🔌 Hermes v2 OAuth voters + release-gate hardening (3.4.7–3.4.8)

**Teeth (3 hosts — was Grok-only in 3.5.0):**
🦷 defer → gate → Task on **Grok, Hermes, OpenCode** (3.5.1)
📋 `pending-delegations` + spawn todo persistence (3.5.0)

```bash
npm i 0xray@3.5.1 @0xray/repertoire@0.1.3
```

Postinstall wires all 4 bridges — no extra `grok install`. Restart Grok MCP sessions after upgrade.

4/4 suit verify · OpenClaw = brain only (no host veto yet)

#0xRay #MCP #LeadDev #GrokBuild

---

## Tweet (short / alt — ~280 chars)

Since v3.4.1: 0xRay **v3.5.1** — honest brain on 4 bridges, delegation **gates on Grok + Hermes + OpenCode**. repertoire **v0.1.3** — suit verify from npm.

3.4.9 deferrals · 3.4.10 routing · 3.5.0 loop · 3.5.1 multi-host teeth

`npm i 0xray@3.5.1 @0xray/repertoire@0.1.3`

#0xRay #MCP

---

## Diff table (for thread / blog)

| Was (last post) | Now |
|-----------------|-----|
| 0xRay **3.4.1** zero-config install | **3.5.1** multi-host delegation gates |
| repertoire **0.1.1** suit verify in npm | **0.1.3** hoist fix + `^3.5.1` + 4-bridge matrix |
| Orchestrator could look "done" on defer | **3.4.9** `success: false` until host spawns |
| Agent routing drift | **3.4.10** `routeSubagent` SSOT + unified complexity |
| Loop enforcement Grok-only (3.5.0) | **3.5.1** Hermes `pre_tool_call` block + OpenCode `tool.execute.before` |
| MEMORY_ROUTING silent no-op risk | **3.4.4** researcher trap routing e2e |

### 0xRay version arc (3.4.1 → 3.5.1)

| Ver | Headline |
|-----|----------|
| 3.4.7 | Consumer-safe publish + upgrade merge smoke |
| 3.4.8 | Hermes v2 OAuth + headless governance |
| 3.4.9 | **Honest orchestrator** — defer ≠ complete |
| 3.4.10 | **Routing SSOT** — implement → backend-engineer |
| 3.5.0 | **Grok loop closure** — pending-delegations + spawn todos |
| 3.5.1 | **Multi-host gates** — Grok / Hermes / OpenCode parity |

### @0xray/repertoire arc (0.1.1 → 0.1.3)

| Ver | Headline |
|-----|----------|
| 0.1.1 | `npx repertoire-verify-suit` ships in tarball |
| 0.1.2 | Consumer pin `0xray@^3.5.0` |
| 0.1.3 | Hoisted `node_modules/0xray` resolve fix · steps 11–13 delegation gate verify · `^3.5.1` |

---

## Thread hooks (optional replies)

**Reply 2 — Two layers (updated)**

Brain: all 4 bridges get honest deferrals + SSOT on disk + memory routing. Teeth: 3 hosts now block writes while pending (OpenClaw observability-only until platform veto API).

**Reply 3 — When you *do* need explicit install**

Only if postinstall was skipped (`--ignore-scripts`) or hooks drifted after upgrade:

```bash
npx 0xray grok install --force   # or hermes / opencode / openclaw
```

Normal path: `npm i` → postinstall wires everything. Restart MCP sessions — hooks don't hot-reload.

**Reply 4 — Verify (no lies)**

```bash
npx repertoire-verify-suit          # Grok consumer
npm run verify:suit:all             # clone repertoire — 4 bridges
node node_modules/0xray/scripts/mjs/verify-hermes-delegation-gate.mjs
```

**Reply 5 — What didn't change**

Still no auto-spawn (platform constraint). Still lead-dev + `analyze-complexity` intake. Opt out: `"auto_chain_delegations": false`.

---

## @0xrayai quote RT (draft)

0xRay **v3.5.1** + repertoire **v0.1.3** — the diff since 3.4.1: honest brain everywhere, delegation gates on Grok/Hermes/OpenCode, suit verify from npm.

`npm i 0xray@3.5.1 @0xray/repertoire@0.1.3`